import { lazy, Suspense, useEffect, useState } from 'react';
import { ArrowDownToLine, ArrowRight, Award, Backpack, BookOpen, ChartNoAxesCombined, Check, CheckCircle2, ChevronDown, ChevronRight, CircleHelp, Compass, Crown, Flame, Footprints, Gem, Gift, GraduationCap, Heart, Home, Leaf, Lightbulb, LockKeyhole, Map, Menu, Plus, ShieldCheck, Sparkles, Sprout, Star, Tent, TreePine, Volume2, VolumeX, X } from 'lucide-react';
import { Character, EmptyState, Logo, Modal, Stars } from './components';
import { playSound, stopSpeaking } from './audio';
import GradePicker from './GradePicker';
import WorldPicker from './WorldPicker';
import Clubhouse from './Clubhouse';
import { chooseRoomItem } from './clubhouse';
import { rememberDiscovery, recommendedSkill, skillMemory } from './learning';
import StorybookBackdrop from './StorybookBackdrop';
import LandingWildlife, { SkySun } from './LandingWildlife';
import { availableSkills, completedMissions, dateKey, defaultSave, gradeLabel, missions, missionSkill, nextMission, readSave, recordRun, STORAGE_KEY, streak, type Run, type Save, type Skill } from './game';

const Quest = lazy(() => import('./Quest'));
type Page = 'adventure' | 'practice' | 'backpack' | 'progress' | 'clubhouse';
type Overlay = 'profile' | 'parents' | 'worlds' | 'help' | 'grades' | 'daily' | null;
const navigation = [
  { id: 'adventure' as Page, label: 'My adventure', icon: Map },
  { id: 'practice' as Page, label: 'Practice camp', icon: Tent },
  { id: 'clubhouse' as Page, label: 'My clubhouse', icon: Home },
  { id: 'backpack' as Page, label: 'My backpack', icon: Backpack },
  { id: 'progress' as Page, label: 'My progress', icon: ChartNoAxesCombined },
];
const skillDetails: Record<Skill, { symbol: string; title: string; description: string; className: string }> = {
  addition: { symbol: '+', title: 'Better together', description: 'Bring groups together and watch them grow.', className: 'green' },
  subtraction: { symbol: '−', title: 'A little less', description: 'Take some away. Discover what stays.', className: 'peach' },
  multiplication: { symbol: '×', title: 'Groups of magic', description: 'Make equal groups. Find a faster way to count.', className: 'lilac' },
  division: { symbol: '÷', title: 'A fair share', description: 'Share the magic so everyone gets the same.', className: 'blue' },
  fractions: { symbol: '½', title: 'Pieces of a whole', description: 'Put equal pieces together to make something whole.', className: 'peach' },
  decimals: { symbol: '.1', title: 'The little numbers', description: 'Explore the wonderful world between whole numbers.', className: 'lilac' },
};

function DailyQuests({ save, onStart, onClaim }: { save: Save; onStart: (skill?: Skill) => void; onClaim: (key: string) => void }) {
  const today = dateKey(); const runs = save.runs.filter(r => r.date === today);
  const tasks = [
    { key: `${today}:mission`, icon: FlagIcon, label: 'A little adventure', desc: 'Complete a story mission', progress: Math.min(1, runs.filter(r => !r.practice).length), total: 1, reward: 10, skill: undefined },
    { key: `${today}:practice`, icon: Plus, label: 'Make some magic', desc: 'Try a round at practice camp', progress: Math.min(1, runs.filter(r => r.practice).length), total: 1, reward: 10, skill: 'addition' as Skill },
    { key: `${today}:stars`, icon: Star, label: 'Reach for the stars', desc: 'Earn 6 adventure stars', progress: Math.min(6, runs.reduce((s, r) => s + r.stars, 0)), total: 6, reward: 15, skill: undefined },
  ];
  return <section className="daily-section"><div className="section-heading"><div><h2>Little quests, big possibilities<span className="heading-star">✳</span></h2><p>A little curiosity every day goes a long way.</p></div><span className="today-label"><span />Today’s quests</span></div><div className="daily-grid">{tasks.map((task, i) => {
    const claimed = save.claimed.includes(task.key); const ready = task.progress >= task.total;
    return <button className={`daily-card daily-${i} ${claimed ? 'claimed' : ''}`} key={task.key} onClick={() => ready ? !claimed && onClaim(task.key) : onStart(task.skill)} disabled={claimed}>
      <span className="daily-icon"><task.icon size={23} /></span><span className="daily-content"><strong>{task.label}</strong><span>{claimed ? 'Reward collected. Nicely done!' : ready ? 'You did it! Tap to collect your gems.' : task.desc}</span><span className="mini-progress"><span style={{ width: `${task.progress / task.total * 100}%` }} /></span></span><span className="daily-reward">{claimed ? <CheckCircle2 size={20} /> : <><Gem size={15} />{task.reward}</>}<small>{task.progress}/{task.total}</small></span>
    </button>;
  })}</div></section>;
}

function FlagIcon({ size = 24 }: { size?: number }) { return <Footprints size={size} />; }

function AdventureMap({ save, onStart, onWorlds, onDaily }: { save: Save; onStart: (index: number) => void; onWorlds: () => void; onDaily: () => void }) {
  const completed = completedMissions(save); const current = nextMission(save);
  const [selected, setSelected] = useState(current);
  useEffect(() => setSelected(current), [current, save.grade]);
  const mission = missions[selected];
  const positions = [{ left: '19%', top: '73%' }, { left: '36%', top: '50%' }, { left: '57%', top: '51%' }, { left: '73%', top: '30%' }, { left: '88%', top: '19%' }];
  return <section className="journey-section" aria-label="Your adventure in Whispering Woods"><div className="section-heading journey-heading"><div><h2>Your next adventure adds up.</h2><p>A world of wonder. One little discovery at a time.</p></div><div className="journey-tools"><button className="button secondary daily-quests-trigger" aria-haspopup="dialog" onClick={onDaily}><Gift size={19} /><span>Little quests</span></button><button className="button small secondary" onClick={onWorlds}><Map size={16} />All worlds<ChevronDown size={15} /></button></div></div>
    <div className="journey-grid"><div className="map-card"><div className="map-card-header"><div className="world-icon"><TreePine size={22} /></div><div><span className="world-eyebrow">WORLD 01</span><h3>Whispering Woods</h3></div><span className="world-completion">{completed.length}<span> / 5 chapters</span><div className="world-progress"><span style={{ width: `${completed.length * 20}%` }} /></div></span></div>
      <div className="world-map"><img className="map-art" src="/art/whispering-woods.png" alt="A winding trail through an enchanted woodland, over a turquoise stream to a magical treehouse" fetchPriority="high" />
        <div className="map-top-note"><Leaf size={13} /><span>{completed.length === 5 ? 'A little magic, everywhere.' : 'Every step brings the forest to life.'}</span></div>
        <svg className="map-route" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true"><path vectorEffect="non-scaling-stroke" d="M19 73C25 69 24 51 36 50S48 57 57 51S62 36 73 30S81 22 88 19" /></svg>
        {missions.map((m, i) => {
          const done = completed.includes(i); const locked = i > current; const best = Math.max(0, ...save.runs.filter(r => !r.practice && r.grade === save.grade && r.mission === i).map(r => r.stars));
          return <div className={`map-stop ${done ? 'done' : locked ? 'locked' : 'current'} ${selected === i ? 'selected' : ''}`} style={positions[i]} key={m.short}>
            {i === current && !done && <span className={`map-trail-buddy ${i === 4 ? 'buddy-at-canopy' : ''}`} aria-hidden="true"><span className="buddy-trail-shadow" /><Character name={save.companion} outfit={save.clubhouse.equipped.outfit} /><span className="buddy-trail-spark">✦</span></span>}
            <button aria-label={`${m.short}${locked ? ', locked. Complete the previous chapter first' : done ? ', completed. Replay chapter' : ', start chapter'}`} aria-disabled={locked} onClick={() => { if (!locked) setSelected(i); }} className="map-node">{done ? <Check size={25} strokeWidth={3} /> : locked ? <LockKeyhole size={20} /> : <span>{i + 1}</span>}</button>
            <span className="map-stop-label">{m.short}</span>{done && <Stars count={best} size={12} />}
          </div>;
        })}
        <div className="map-bottom-caption"><span className="map-legend-dot" />{completed.length === 5 ? 'Woodland guardian' : 'The adventure begins with you'}</div>
        <button className="map-compass" aria-label="Explore all worlds" onClick={onWorlds}><Compass size={27} strokeWidth={1.4} /></button>
      </div><div className="map-footer"><span><Sprout size={15} />Little steps. Legendary adventures.</span><div><span className="legend-complete" />Completed<span className="legend-next" />Up next</div></div>
    </div>
    <aside className="mission-card"><div className="mission-kicker"><span className="live-dot" />{completed.includes(selected) ? 'PLAY IT AGAIN' : 'YOUR NEXT CHAPTER'}<BookOpen size={17} /></div><div className="mission-mascot"><span className="mascot-disc" /><Character name={mission.companion} /><Sparkles className="mascot-spark" size={20} /><span className="mascot-name">Meet {mission.companion}!</span></div><h3>{mission.title}</h3><p>{mission.story}</p><div className="mission-skill"><span className="skill-symbol">{skillDetails[missionSkill(save.grade, selected)].symbol}</span><div><strong>{missionSkill(save.grade, selected).charAt(0).toUpperCase() + missionSkill(save.grade, selected).slice(1)} magic</strong><span>{save.grade <= 1 ? 'Count it. See it. Make it happen.' : 'A little thinking unlocks a lot.'}</span></div></div><div className="mission-rewards"><span><Star size={15} />100 XP for finishing</span><span><Gift size={15} />A surprise!</span></div><button className="button primary full" onClick={() => onStart(selected)}>{completed.includes(selected) ? 'Play again' : 'Let’s play'}<ArrowRight size={20} /></button><span className="mission-duration">5 little challenges · At your own pace</span></aside>
    </div>
  </section>;
}

function Practice({ save, onStart }: { save: Save; onStart: (skill: Skill) => void }) {
  const recommended = recommendedSkill(save); const memory = skillMemory(save, recommended);
  return <section className="content-page"><div className="page-banner practice-banner"><Tent size={53} strokeWidth={1.3} /><div><span className="skill-tag">A LITTLE ROOM TO GROW</span><h1>Welcome to practice camp.</h1><p>No rush, no pressure. Just you, your buddy, and a little math magic.</p></div></div><div className="memory-invitation"><Character outfit={save.clubhouse.equipped.outfit}/><div><h3>{memory.reviews.length ? "Milo saved a little discovery for you." : memory.completed ? "Let’s pick up where we left off." : "A little adventure, just for you."}</h3><p>{memory.reviews.length ? `A fresh way to explore ${recommended}. We’ll build it together, then try something new.` : `Your ${recommended} adventure starts with a gentle warm-up. Milo remembers how you’re growing.`}</p></div><button className="button primary" onClick={()=>onStart(recommended)}>Let’s try it<ArrowRight size={16}/></button></div><div className="section-heading"><div><h2>What shall we discover?</h2><p>Pick a skill. Every round has five fresh adventures.</p></div><span className="today-label">{gradeLabel(save.grade)}</span></div><div className="practice-grid">{availableSkills(save.grade).map(skill => {
    const detail = skillDetails[skill]; const runs = save.runs.filter(r => r.grade === save.grade && r.skill === skill);
    return <button className={`practice-card ${detail.className}`} key={skill} onClick={() => onStart(skill)}><span className="practice-symbol">{detail.symbol}</span><span className="skill-tag">{skill}</span><h3>{detail.title}</h3><p>{detail.description}</p><span className="practice-bottom"><span>{runs.length ? `${runs.length} rounds explored` : 'Something new to discover'}</span><ArrowRight size={21} /></span></button>;
  })}</div><div className="learning-note"><Lightbulb size={24} /><div><h3>Your brain loves a little “again.”</h3><p>Come back to a skill you’ve tried before. Remembering how to do it is part of getting stronger.</p></div></div></section>;
}

function BackpackPage({ save, onCompanion, onAdventure }: { save: Save; onCompanion: (name: string) => void; onAdventure: () => void }) {
  const completed = [...new Set(save.runs.filter(r => !r.practice).map(r => r.mission))];
  return <section className="content-page"><div className="section-heading large-heading"><div><h1>A backpack full of stories.</h1><p>Every little treasure holds a big adventure.</p></div><span className="treasure-count"><Gem size={21} />{save.gems} gems</span></div>
    <h2 className="subsection-title">Better with a buddy</h2><div className="companion-grid">{[{ name: 'Milo', role: 'The curious fox', need: 0, description: 'A big heart, a tiny satchel, and a nose for adventure.' }, { name: 'Pip', role: 'The gentle dragon', need: 2, description: 'A little shy. A little clumsy. A whole lot of brave.' }, { name: 'Lumi', role: 'The starlight keeper', need: 4, description: 'Proof that even the smallest friend can light the way.' }].map(c => {
      const locked = completed.length < c.need;
      return <div className={`companion-card ${locked ? 'is-locked' : ''}`} key={c.name}><Character name={c.name} /><div><h3>{c.name}<span>{c.role}</span></h3><p>{c.description}</p></div><button className={`button ${save.companion === c.name ? 'primary' : 'secondary'}`} disabled={locked} onClick={() => onCompanion(c.name)}>{locked ? <><LockKeyhole size={15} />Complete {c.need} chapters</> : save.companion === c.name ? <><Check size={17} />My adventure buddy</> : 'Choose as my buddy'}</button></div>;
    })}</div><h2 className="subsection-title">Treasures from the trail <span>{completed.length} / 5</span></h2>{!completed.length ? <EmptyState title="Your first keepsake is out there." text="Finish a story chapter to bring home a little piece of Numberia." action="Let’s find it" onAction={onAdventure} /> : <div className="treasure-grid">{missions.map((m, i) => <div className={`treasure-card ${completed.includes(i) ? '' : 'is-locked'}`} key={m.item}><span className={`treasure-object treasure-${i}`}>{i === 0 ? <Lightbulb size={38} /> : i === 1 ? <Award size={38} /> : i === 2 ? <Backpack size={38} /> : i === 3 ? <Sparkles size={38} /> : <Crown size={38} />}</span><h3>{m.reward}</h3><span>{completed.includes(i) ? m.short : `Discover in chapter ${i + 1}`}</span>{completed.includes(i) ? <CheckCircle2 size={17} /> : <LockKeyhole size={17} />}</div>)}</div>}
  </section>;
}

function ProgressPage({ save, onAdventure }: { save: Save; onAdventure: () => void }) {
  const runs = save.runs.filter(r => r.grade === save.grade);
  return <section className="content-page"><div className="section-heading large-heading"><div><h1>Look how far you’ve grown.</h1><p>Big things happen one little discovery at a time.</p></div><span className="today-label">{gradeLabel(save.grade)}</span></div><div className="stats-grid">{[{ icon: Footprints, value: runs.length, label: 'Adventures completed' }, { icon: Star, value: runs.reduce((s, r) => s + r.stars, 0), label: 'Adventure stars earned' }, { icon: Sparkles, value: runs.length * 5, label: 'Challenges figured out' }, { icon: Flame, value: streak(save.runs), label: 'Days on your current streak' }].map(s => <div className="stat-card" key={s.label}><s.icon size={23} /><strong>{s.value}</strong><span>{s.label}</span></div>)}</div>
    {!runs.length ? <EmptyState title="This is where your story grows." text="Take your first adventure, and we’ll keep track of all the things you’re learning." action="Start my adventure" onAction={onAdventure} /> : <><h2 className="subsection-title">Your math garden</h2><div className="mastery-list">{availableSkills(save.grade).map(skill => { const attempts = runs.filter(r => r.skill === skill); const ratio = Math.min(1, attempts.length / 5); return <div className="mastery-row" key={skill}><span className={`skill-symbol ${skillDetails[skill].className}`}>{skillDetails[skill].symbol}</span><div><strong>{skill.charAt(0).toUpperCase() + skill.slice(1)}</strong><span>{!attempts.length ? 'Ready to plant your first seed' : ratio >= 0.8 ? 'Blooming beautifully' : 'Growing with every try'}</span></div><div className="mastery-bar"><span style={{ width: `${ratio * 100}%` }} /></div><span>{attempts.length} rounds</span></div>; })}</div><p className="progress-explanation">Every completed round helps your garden grow. Hints, help, and brave tries count just as much. Keep exploring!</p><h2 className="subsection-title">Footprints on the trail</h2><div className="activity-list">{[...runs].reverse().slice(0, 8).map(r => <div className="activity-row" key={r.id}><span className="activity-icon">{r.practice ? <Tent size={22} /> : <Map size={22} />}</span><div><strong>{r.practice ? `${r.skill.charAt(0).toUpperCase() + r.skill.slice(1)} practice` : missions[r.mission].title}</strong><span>{new Date(`${r.date}T12:00:00`).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })} · 5 discoveries · You kept going!</span></div><Stars count={r.stars} /><strong className="activity-xp">+{r.xp} XP</strong></div>)}</div></>}
  </section>;
}

export default function App() {
  const [save, setSave] = useState<Save>(() => { try { return readSave(localStorage.getItem(STORAGE_KEY)); } catch { return { ...defaultSave, runs: [], claimed: [] }; } });
  const [page, setPage] = useState<Page>('adventure'); const [overlay, setOverlay] = useState<Overlay>(null);
  const [quest, setQuest] = useState<{ id: string; index: number; practiceSkill?: Skill } | null>(null);
  const [mobileMenu, setMobileMenu] = useState(false); const [nameDraft, setNameDraft] = useState(save.name);
  const [saveError, setSaveError] = useState(false); const [parentTab, setParentTab] = useState<'overview' | 'learning'>('overview');
  useEffect(() => { try { localStorage.setItem(STORAGE_KEY, JSON.stringify(save)); setSaveError(false); } catch { setSaveError(true); } }, [save]);
  const goTo = (p: Page) => { setPage(p); setMobileMenu(false); window.scrollTo({ top: 0, behavior: 'instant' }); };
  const start = (index = nextMission(save), practiceSkill?: Skill) => setQuest({ id: crypto.randomUUID(), index, practiceSkill });
  useEffect(() => { if (!save.sound) stopSpeaking(); }, [save.sound]);
  const level = Math.floor(save.xp / 300) + 1; const nextLevelXP = save.xp % 300;
  const dailyClaim = (key: string) => setSave(s => s.claimed.includes(key) ? s : { ...s, gems: s.gems + (key.endsWith(':stars') ? 15 : 10), claimed: [...s.claimed, key] });
  const complete = (run: Run) => setSave(s => recordRun(s, run));
  const exportProgress = () => {
    const data = new Blob([JSON.stringify(save, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(data); const a = document.createElement('a'); a.href = url; a.download = 'numberia-progress.json'; a.click(); setTimeout(() => URL.revokeObjectURL(url), 500);
  };
  return <><StorybookBackdrop />{page === 'adventure' && <LandingWildlife />}<a className="skip-link" href="#main-content">Skip to adventure</a><div className={`app-shell ${page === 'adventure' ? 'home-adventure' : ''}`} onClick={e => { if (save.sound && (e.target as HTMLElement).closest('button')) playSound('tap'); }}>
    <aside className={`sidebar ${mobileMenu ? 'menu-open' : ''}`}><div className="sidebar-brand"><button className="brand-button" aria-label="Numberia home" onClick={() => goTo('adventure')}><Logo /></button><button className="icon-button close-menu" aria-label="Close menu" onClick={() => setMobileMenu(false)}><X size={20} /></button></div><p className="brand-tagline">Little minds. Big adventures.</p>
      <nav className="main-nav" aria-label="Main navigation">{navigation.map(item => <button key={item.id} className={`nav-link ${page === item.id ? 'active' : ''}`} aria-current={page === item.id ? 'page' : undefined} onClick={() => goTo(item.id)}><item.icon size={21} strokeWidth={page === item.id ? 2.3 : 1.8} /><span>{item.label}</span>{item.id === 'backpack' && completedMissions(save).length > 0 && <span className="nav-count">{completedMissions(save).length}</span>}</button>)}</nav>
      <div className="sidebar-journey"><div className="sidebar-label">YOUR EXPLORER JOURNEY</div><div className="explorer-level"><span className="level-medallion"><Sprout size={24} /></span><div><strong>{level < 3 ? 'Curious explorer' : level < 6 ? 'Brave trailblazer' : 'Woodland guardian'}</strong><span>Level {level}</span></div></div><div className="xp-label"><span>{nextLevelXP} XP</span><span>{300} XP</span></div><div className="progress-track"><span style={{ width: `${nextLevelXP / 3}%` }} /></div><p>A little closer with every discovery.</p></div>
      <div className="sidebar-bottom"><div className="buddy-card"><Character name={save.companion} outfit={save.clubhouse.equipped.outfit} /><div className="buddy-bubble">“Big adventures start<br />with little steps.”</div><p>{save.companion} believes in you.</p></div><button className="parent-link" onClick={() => { setOverlay('parents'); setMobileMenu(false); }}><ShieldCheck size={19} />Grown-up corner<ChevronRight size={16} /></button><button className="help-link" onClick={() => { setOverlay('help'); setMobileMenu(false); }}><CircleHelp size={16} />A little help</button></div>
    </aside>{mobileMenu && <button className="menu-scrim" aria-label="Close navigation" onClick={() => setMobileMenu(false)} />}
    <div className="main-shell"><header className="topbar"><div className="topbar-title"><button className="icon-button mobile-menu-button" aria-label="Open navigation" onClick={() => setMobileMenu(true)}><Menu size={22} /></button><span>{navigation.find(n => n.id === page)?.label}</span><span className="topbar-divider" /><button className="grade-select grade-picker-trigger" aria-label="Learning grade" aria-haspopup="dialog" onClick={() => setOverlay('grades')}><GraduationCap size={19} /><span>{gradeLabel(save.grade)}</span><ChevronDown size={15} /></button></div><div className="topbar-actions"><span className="top-stat streak-stat" title="Days on your learning streak"><Flame size={20} /><strong>{streak(save.runs)}</strong><span>day streak</span></span><button className="top-stat gems-stat" aria-label={`Decorate my clubhouse with ${save.gems} gems`} onClick={()=>goTo('clubhouse')}><Gem size={20} /><strong>{save.gems}</strong></button><button className="icon-button sound-button" aria-label={save.sound ? 'Turn sound off' : 'Turn sound on'} aria-pressed={save.sound} onClick={() => setSave(s => ({ ...s, sound: !s.sound }))}>{save.sound ? <Volume2 size={20} /> : <VolumeX size={20} />}</button><button className="profile-button" aria-label="Edit explorer profile" onClick={() => { setNameDraft(save.name); setOverlay('profile'); }}><span className="profile-avatar"><Character name={save.companion} outfit={save.clubhouse.equipped.outfit} /></span><ChevronDown size={14} /></button></div></header>
      <main id="main-content" className="main-content">{saveError && <div className="storage-warning" role="alert">Your browser can’t save progress right now. Keep this tab open, or download your progress in the grown-up corner.</div>}
        {page === 'adventure' && <><SkySun /><section className="welcome-banner"><div className="welcome-copy"><span className="welcome-eyebrow"><span />A LITTLE MATH. A LOT OF MAGIC.</span><h1>{save.runs.length ? `Welcome back, ${save.name === 'Explorer' ? 'explorer' : save.name}!` : 'A big adventure starts small.'}</h1><p>The forest is full of stories. And you’re the missing piece.<br /><span>Ready to make a little magic?</span></p><button className="welcome-link" onClick={() => start()}>Your adventure is waiting<ArrowRight size={17} /></button></div><div className="welcome-art"><span className="hero-flower flower-one">✳</span><span className="hero-flower flower-two">✳</span><img src="/art/explorers.png" alt="A smiling young explorer with Milo the fox and Pip the little dragon" /><span className="hello-sticker">Oh, hello you!</span></div></section><AdventureMap save={save} onStart={i => start(i)} onWorlds={() => setOverlay('worlds')} onDaily={() => setOverlay('daily')} /></>}
        {page === 'practice' && <Practice save={save} onStart={skill => start(0, skill)} />}
        {page === 'clubhouse' && <Clubhouse save={save} onChoose={id => setSave(s => chooseRoomItem(s,id))} onName={petName => setSave(s => ({...s,clubhouse:{...s.clubhouse,petName}}))} onAdventure={() => start()} />}
        {page === 'backpack' && <BackpackPage save={save} onCompanion={companion => setSave(s => ({ ...s, companion }))} onAdventure={() => goTo('adventure')} />}
        {page === 'progress' && <ProgressPage save={save} onAdventure={() => goTo('adventure')} />}
      </main>
    </div>
  </div>
  {quest && <Suspense fallback={<Modal title="Packing your adventure" onClose={() => setQuest(null)}><div className="loading-state"><Sprout size={36} /><h2>Packing a little magic…</h2></div></Modal>}><Quest key={quest.id} save={save} missionIndex={quest.index} practiceSkill={quest.practiceSkill} onClose={() => setQuest(null)} onComplete={complete} onLearn={observation => setSave(s => rememberDiscovery(s, observation))} onClubhouse={() => {setQuest(null);goTo('clubhouse');}} onSoundChange={() => setSave(s => ({ ...s, sound: !s.sound }))} onContinue={() => { if (quest.practiceSkill) start(quest.index, quest.practiceSkill); else if (quest.index < 4) start(quest.index + 1); else setQuest(null); }} /></Suspense>}
  {overlay === 'daily' && <Modal title="Today’s little quests" onClose={() => setOverlay(null)} className="daily-quests-modal"><DailyQuests save={save} onStart={skill => { setOverlay(null); start(nextMission(save), skill); }} onClaim={dailyClaim} /></Modal>}
  {overlay === 'grades' && <Modal title="Choose your learning grade" onClose={() => setOverlay(null)} className="grades-modal"><div className="grade-picker-welcome"><Character /><span className="skill-tag">YOUR NEXT BIG LITTLE ADVENTURE</span><h1>Ready, set… grow!</h1><p>Pick your grade. Milo will meet you there.</p></div><GradePicker grade={save.grade} onSelect={grade => { setSave(s => ({ ...s, grade })); if (save.sound) playSound('open'); setOverlay(null); }} /><p className="grade-picker-footnote"><Heart size={15} />Change your grade anytime. Your treasures stay with you.</p></Modal>}
  {overlay === 'profile' && <Modal title="Your explorer profile" onClose={() => setOverlay(null)} className="profile-modal"><Character name={save.companion} outfit={save.clubhouse.equipped.outfit} /><h1>Hello, little adventurer.</h1><p>Every great story starts with a name.</p><form onSubmit={e => { e.preventDefault(); setSave(s => ({ ...s, name: nameDraft.trim() || 'Explorer' })); setOverlay(null); }}><label>Your explorer name<input autoFocus maxLength={24} value={nameDraft} onChange={e => setNameDraft(e.target.value)} placeholder="Explorer" /></label><GradePicker compact grade={save.grade} onSelect={grade => setSave(s => ({ ...s, grade }))} /><p className="form-helper">You can change your level anytime. Your adventures at each level stay saved.</p><button className="button primary full" type="submit">That’s me!<Check size={18} /></button></form></Modal>}
  {overlay === 'worlds' && <Modal title="The world of Numberia" onClose={() => setOverlay(null)} className="worlds-modal"><WorldPicker completed={completedMissions(save).length} onExplore={() => { setOverlay(null); goTo('adventure'); }} /></Modal>}
  {overlay === 'help' && <Modal title="A little help" onClose={() => setOverlay(null)}><div className="info-modal"><CircleHelp size={34} /><h1>A little help along the way.</h1><div className="help-step"><span>1</span><div><h3>Follow your curiosity.</h3><p>Pick your grade, then press “Let’s play” on the adventure map. The first chapter is ready for you.</p></div></div><div className="help-step"><span>2</span><div><h3>Make a little math magic.</h3><p>Drag berries into a basket, build a bridge, or share snacks equally. You can also tap an object, then its basket. Switch to Explore & answer to roam the park with Milo and use the big answer buttons.</p></div></div><div className="help-step"><span>3</span><div><h3>Watch your story grow.</h3><p>Finish five challenges to earn all three stars and a keepsake. Hints and retries never reduce your rewards. New chapters and buddies unlock as you explore.</p></div></div><div className="milo-note"><Lightbulb size={28} /><p>Stuck? Ask for a hint. There are no timers, lost lives, or lost stars. Every try is a little step forward.</p></div><button className="button primary full" onClick={() => setOverlay(null)}>I’m ready to explore<ArrowRight size={19} /></button></div></Modal>}
  {overlay === 'parents' && <Modal title="Grown-up corner" onClose={() => setOverlay(null)} className="parents-modal"><span className="skill-tag"><ShieldCheck size={16} /> GROWN-UP CORNER</span><h1>A little insight into their adventure.</h1><div className="modal-tabs"><button className={parentTab === 'overview' ? 'active' : ''} onClick={() => setParentTab('overview')}>Their journey</button><button className={parentTab === 'learning' ? 'active' : ''} onClick={() => setParentTab('learning')}>How learning works</button></div>{parentTab === 'overview' ? <><div className="parent-summary"><Character name={save.companion} outfit={save.clubhouse.equipped.outfit} /><div><h2>{save.name}</h2><p>{gradeLabel(save.grade)} · Level {level} explorer</p><span>{save.runs.length * 5} challenges completed · {save.xp} XP earned</span></div></div><p className="parent-copy">Progress stays in this browser. There are no accounts, ads, real-money purchases, public rankings, or time limits. Switching grades keeps each grade’s chapter progress.</p><div className="parent-actions"><button className="button secondary" onClick={() => { setOverlay(null); goTo('progress'); }}><ChartNoAxesCombined size={18} />View learning progress</button><button className="button secondary" onClick={exportProgress}><ArrowDownToLine size={18} />Download progress</button></div></> : <div className="learning-details"><h3>See it, think it, try it.</h3><p>Animated berry groups connect quantities to written arithmetic. Milo offers problem-specific spoken coaching and demonstrations. Every completed round earns 3 stars, 100 XP, and 15 gems; hints and retries never reduce rewards. Learning is saved separately for each grade and skill, including between visits. After two independent discoveries, the challenge grows; after support, it gently eases. Supported facts return after other practice, and every visit begins with a warm-up. Decorating the clubhouse uses earned gems only. Practice asks children to retrieve an answer, with hints and another try when needed.</p><h3>Practice grows with the learner.</h3><p>K–2 covers addition and subtraction; grades 3–4 add multiplication and division; grade 5 also explores same-denominator fractions and tenths. Grade labels are starting points, not a complete curriculum or an assessment.</p><h3>Helpful color. Purposeful movement.</h3><p>Colors consistently distinguish groups, and each answer has a number and text feedback. Movement celebrates restored objects. Reduced-motion preferences are respected. We don’t claim a particular color palette or this game has been clinically validated.</p><h3>Built around established learning guidance.</h3><p>The approach draws on the Institute of Education Sciences’ guidance on <a href="https://ies.ed.gov/ncee/wwc/practiceguide/1" target="_blank" rel="noreferrer">retrieval practice and connecting concrete and abstract representations</a>, and <a href="https://ies.ed.gov/ncee/wwc/practiceguide/26" target="_blank" rel="noreferrer">representations in elementary mathematics</a>.</p></div>}</Modal>}
  </>;
}
